<%= simple_form_for(@admin_interactive_video) do |f| %>
  <%= render 'shared/form_errors', object: @admin_interactive_video %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :language_code_id, collection: LanguageCode.all, label_method: :label, value_method: :id, prompt: t(:please_select) %>
    <%= f.input :description %>
    <%= f.input :content %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>

<% end %>
