<%= simple_form_for(@admin_operator) do |f| %>
  <%= render 'shared/form_errors', object: @admin_operator %>

  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :country, :as => :string %>
  <%= f.input :reg_code_prefix %>
  <%= f.input :disabled %>
  
  <%= f.button :submit %>

<% end %>
