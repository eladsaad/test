<%= form_for(@admin_system_admin) do |f| %>
  <% if @admin_system_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_system_admin.errors.count, "error") %> prohibited this admin_system_admin from being saved:</h2>

      <ul>
      <% @admin_system_admin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :first_name %><br>
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br>
    <%= f.text_field :last_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
